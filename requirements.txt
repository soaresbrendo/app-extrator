# Core Flask dependencies
Flask==3.0.3
Werkzeug==3.0.3

# PDF processing - using PyPDF2 (pure Python, no compilation needed)
PyPDF2==3.0.1

# Excel generation (lightweight, no compilation needed)
openpyxl==3.1.2

# Production server
gunicorn==22.0.0
</merged_code
